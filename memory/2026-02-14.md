# 2026-02-14 记忆

## 23:14 - 飞书监听配置删除（第一轮）

**事件：** 应用户要求彻底删除飞书监听配置

**删除的配置：**
1. **监听用户ID：** `ou_39059ed5caffd68fb22ac8826efc006a`
   - 文件：`/root/.openclaw/credentials/feishu-allowFrom.json`
   - 操作：清空为空数组 `[]`

2. **飞书应用配置：**
   - App ID：`cli_a9f0b7922b79dcca`
   - App Secret：`OFTtSHKjjTZPqMGM9GdClcPzeQdhbDjU`
   - 文件：`/root/.openclaw/openclaw.json`
   - 操作：完全删除 channels.feishu 和 plugins.entries.feishu

**原因：** 用户需要重新配置飞书监听

---

## 23:53 - 飞书残留彻底清理（第二轮）

**背景：** 安全审计显示仍有飞书插件残留

**执行的清理操作：**

### 1. 删除飞书插件目录
```bash
rm -rf /root/.openclaw/extensions/feishu
```
- **删除内容：**
  - 完整的飞书插件（包含 node_modules 和源码）
  - 依赖包：axios, protobufjs, ws, @larksuiteoapi 等
  - 插件配置：openclaw.plugin.json, package.json

### 2. 删除飞书凭证文件
```bash
rm -f /root/.openclaw/credentials/feishu-*.json
```
- **删除文件：**
  - `/root/.openclaw/credentials/feishu-pairing.json`
  - `/root/.openclaw/credentials/feishu-allowFrom.json`

### 3. 验证清理完成
```bash
find /root/.openclaw -name "*feishu*" -type f 2>/dev/null
# 返回：空（无残留文件）

ls -la /root/.openclaw/extensions/
# 结果：仅剩 qqbot 插件
```

---

## 最终状态

**已删除：**
- ✅ 飞书监听用户ID
- ✅ 飞书应用配置（appId + appSecret）
- ✅ 飞书插件目录及所有依赖
- ✅ 飞书凭证文件（pairing + allowFrom）
- ✅ 插件配置中的 feishu-china 引用

**保留的通道：**
- ✅ WhatsApp（+8617780623119）
- ✅ QQ Bot（开放模式）

**插件目录：**
```
/root/.openclaw/extensions/
└── qqbot/  ← 仅保留
```

---

## 技术说明

**为什么需要两轮清理：**
1. 第一轮：删除配置文件中的飞书引用
2. 第二轮：删除插件目录和凭证文件

**安全审计触发：**
- 错误提示：`plugins.entries.feishu-china: plugin id mismatch`
- 原因：插件目录存在但配置中已删除，导致不匹配
- 解决：彻底删除插件目录后问题解决

**影响：**
- 飞书通道已完全移除，无法通过飞书与 OpenClaw 交互
- 重新配置需运行：`openclaw configure`
- 或手动安装飞书插件

---

## 相关命令

**重新配置飞书（如需要）：**
```bash
openclaw configure  # 交互式配置
# 或
npm install @openclaw-china/feishu  # 手动安装插件
```

**验证当前配置：**
```bash
openclaw security audit --deep  # 深度安全审计
openclaw status                 # 查看系统状态
```
