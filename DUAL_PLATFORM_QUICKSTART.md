# 双平台部署 - 快速开始

## 🎯 3分钟完成配置

### 第1步：在 Gitee 创建仓库（1分钟）

1. 访问 https://gitee.com/
2. 点击右上角 **+** → **新建仓库**
3. 仓库名称：`ai-news-hub`
4. **取消勾选**"使用Readme初始化仓库"
5. 点击 **创建**

### 第2步：添加 Gitee 远程仓库（30秒）

```bash
cd /data1/cc/vide-coding/ai-news-hub

# 添加 Gitee 远程仓库（替换为你的用户名）
git remote add gitee https://gitee.com/你的用户名/ai-news-hub.git

# 验证
git remote -v
```

### 第3步：推送到 Gitee（30秒）

```bash
# 推送代码到 Gitee
git push gitee main

# 输入 Gitee 账号密码
```

### 第4步：启用 Gitee Pages（1分钟）

1. 访问你的 Gitee 仓库页面
2. 点击顶部 **服务** → **Gitee Pages**
3. 配置如下：
   - **部署分支**: 选择 `main`
   - **部署目录**: 填写 `docs/.vitepress/dist`
4. 点击 **启动** 按钮
5. 等待 1-3 分钟
6. 访问显示的地址（例如：`https://你的用户名.gitee.io/ai-news-hub/`）

---

## ✅ 完成！

现在您的网站已部署到两个平台：

### 🌍 国际访问
```
https://ai-news-hub-rosy.vercel.app/
```
**优点**：全球CDN、速度快、稳定
**缺点**：国内访问较慢

### 🇨🇳 国内访问
```
https://你的用户名.gitee.io/ai-news-hub/
```
**优点**：国内服务器、访问快速
**缺点**：国际访问较慢

---

## 🚀 日常使用

### 更新网站后部署

#### 方法1：完整部署（推荐）
```bash
cd /data1/cc/vide-coding/ai-news-hub

# 构建并推送到双平台
npm run deploy:dual
```

#### 方法2：快速推送
```bash
# 仅推送（不重新构建）
npm run push:dual
```

#### 方法3：手动推送
```bash
# 推送到 GitHub（触发 Vercel）
git push origin main

# 推送到 Gitee（触发 Gitee Pages）
git push gitee main
```

---

## 🔧 验证配置

运行配置检查脚本：

```bash
cd /data1/cc/vide-coding/ai-news-hub
./check-dual-platform.sh
```

会检查：
- ✅ Gitee 远程仓库是否配置
- ✅ 构建产物是否存在
- ✅ 部署脚本是否可执行
- ✅ npm 命令是否配置
- ✅ Git 工作区状态

---

## 📝 常用命令

```bash
# 查看远程仓库
git remote -v

# 添加 Gitee 远程仓库
git remote add gitee <gitee仓库地址>

# 删除 Gitee 远程仓库
git remote remove gitee

# 完整部署（构建 + 推送）
npm run deploy:dual
./deploy-dual-platform.sh

# 快速推送（不构建）
npm run push:dual
./push-dual-platform.sh

# 仅推送到 GitHub
git push origin main

# 仅推送到 Gitee
git push gitee main

# 同时推送到双平台
git push origin main && git push gitee main

# 本地预览
npm run docs:dev
```

---

## 🛠️ 故障排查

### Gitee 推送失败？

**错误**：`fatal: 'gitee' does not appear to be a git repository`

**解决**：
```bash
# 重新添加远程仓库
git remote add gitee https://gitee.com/你的用户名/ai-news-hub.git
```

---

### Gitee Pages 部署失败？

**可能原因**：
1. 构建目录不正确
2. 分支名称不对
3. 文件过大

**解决**：
1. 确认构建目录：`ls docs/.vitepress/dist/`
2. 检查分支名称：`git branch`（应该是 main）
3. 在 Gitee Pages 页面点击 **更新** 按钮

---

### 双平台内容不一致？

**解决**：
```bash
# 使用完整部署脚本
./deploy-dual-platform.sh

# 或手动构建后推送
npm run docs:build
git add docs/.vitepress/dist/
git commit -m "Build: update"
git push origin main
git push gitee main
```

---

## 📚 更多文档

详细配置说明请查看：
- **完整指南**: `DEPLOY_DUAL_PLATFORM.md`
- **项目说明**: `README.md`

---

## 🎉 享受极速访问！

现在无论用户在国内还是国外，都能快速访问您的网站：

- 🇨🇳 **国内用户** → Gitee Pages（秒开）
- 🌍 **国际用户** → Vercel（全球CDN）
- 💰 **总成本** → 完全免费
- 🚀 **部署方式** → 自动部署

---

**祝使用愉快！** 如有问题，请查看 `DEPLOY_DUAL_PLATFORM.md` 详细文档。
